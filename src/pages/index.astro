---
import { getCollection } from 'astro:content';
import '../styles/global.css';


const posts = await getCollection('ctf');
---


<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>CTF Writeups</title>
  </head>
  <body class="bg-black text-white font-sans px-4 py-8">
    <main class="max-w-3xl mx-auto">
      <h1 class="text-4xl font-bold mb-6">🧠 CTF Writeups</h1>
      

      <ul class="space-y-6">
        {posts.map((post) => (
          <li class="border-b border-gray-700 pb-2">
            <a class="text-cyan-400 text-xl hover:underline" href={`/ctf/${post.slug}`}>
              {post.data.title}
            </a>  
            <p class="text-sm text-gray-400">
              {post.data.platform} — {new Date(post.data.publishedAt).toLocaleDateString()}
            </p>
          </li>
        ))}
      </ul>
    </main>
      
  </body>


</html>
