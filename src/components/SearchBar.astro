---
// components/SearchBar.astro
export const hydrate = 'load';
---
<section class="bg-gray-900 px-6 py-6 text-white" x-data>
  <label for="search" class="block text-lg font-semibold mb-2">🔍 Buscar retos o técnicas</label>
  
  <input
    id="search"
    type="text"
    x-model="$store.search.query"
    placeholder="Ej: snmp, hydra, reverse shell..."
    class="w-full p-3 rounded bg-black text-white border border-gray-700 focus:outline-none focus:ring focus:ring-cyan-400"
  />

  <div class="mt-4 text-sm text-gray-400">
    <p class="mb-1">Ejemplos rápidos:</p>
    <div class="flex flex-wrap gap-3">
      <template x-for="ejemplo in ['HTB', 'THM', 'nmap', 'hydra', 'SQLi', 'reverse shell', 'XSS']">
        <button
          type="button"
          @click="$store.search.query = ejemplo"
          class="bg-cyan-800 text-white px-3 py-1 rounded-full text-xs hover:bg-cyan-700 transition"
          x-text="ejemplo"
        ></button>
      </template>
    </div>
  </div>
</section>
